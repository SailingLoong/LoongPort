# Generated by Cargo
# will have compiled files and executables
#
# **不带尾斜杠** —— `/target/` 只匹配目录，维护者把 target 软链到主树复用构建缓存时
# （多 worktree 并行开发的常规做法）软链不是目录，会以 untracked 出现并被 `git add -A`
# 带进仓里。2026-08-08 实测发生过一次：一个指向维护者本机绝对路径的软链进了 main，
# 别人 clone 下来是条悬空链接。
/target
/gen/schemas

import type { ModelsDevEntry } from "@/types/usage";

export type { ModelsDevEntry };

/** Formats the backend-normalized decimal price for display. */
export function formatPrice(value: string): string {
  return value;
}
